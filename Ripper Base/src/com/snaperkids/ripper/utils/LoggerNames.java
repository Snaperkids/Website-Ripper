/*
 *
 */
package com.snaperkids.ripper.utils;

// TODO: Verify all logger names are accounted for.
// Why were the loggers named again?
// TODO: Write Javadocs
/**
 * The Enum LoggerNames.
 */
public enum LoggerNames {

	/** The configuration. */
	CONFIGURATION,
	/** The download. */
	DOWNLOAD,
	/** The gui. */
	GUI,
	/** The gui output handler. */
	GUI_OUTPUT_HANDLER,
	/** The logger pane. */
	LOGGER_PANE,
	/** The options listener. */
	OPTIONS_LISTENER,
	/** The options pane. */
	OPTIONS_PANE,
	/** The ripper. */
	RIPPER,

	/** The ripper history table. */
	RIPPER_HISTORY_TABLE,
	/** The ripper queue. */
	RIPPER_QUEUE,
	/** The submit button listenter. */
	SUBMIT_BUTTON_LISTENTER,
	/** The user entry pane. */
	USER_ENTRY_PANE;

}
